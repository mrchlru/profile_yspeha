import type { ReactElement, ReactNode } from "react";

/**
 * Маршруты анкеты «ПРОФ СБ + ПРОФ образование».
 */
export default function ProfSbEducationRouteLayout({
  children,
}: Readonly<{
  children: ReactNode;
}>): ReactElement {
  return <>{children}</>;
}
